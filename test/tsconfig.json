{
  "//": "Editor/type-check config for tests. The build (../tsconfig.json) excludes test/; tests run via `tsx --test`.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "..",
    "types": ["node"]
  },
  "include": ["**/*.ts", "../src/**/*.ts"]
}
